package sortKDistinctValLinkedList;

//Given a linked list containing 0s,1s or 2s. Sort it.

public class LinkedListSorter {
	
	/* In this problem we are given that we can have only 0s, 1s, or 2s.
	 * There are good ways to sort this and then there are not so good ways.
	 * One way we can solve this problem is by running through the entire linked list
	 */
	
	
	
}
